<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include
        android:id="@+id/customToolbar"
        layout="@layout/tolbar"></include>

    <FrameLayout
        android:id="@+id/content"
        android:background="@color/colorFundo"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">


        <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_marginLeft="5dp"
            android:layout_marginRight="5dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="120dp"
            android:text="Você não está em nenhuma equipe ainda. Crie uma ou peça para entrar na aba de equipes!"
            android:textAlignment="center"
            android:textColor="@color/colorFundoLogin"
            android:textSize="20dp" />

        <Button
            android:id="@+id/criarEquipe"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Criar"
            android:layout_gravity="center">

        </Button>

    </FrameLayout>

    <android.support.design.widget.BottomNavigationView
        android:id="@+id/bottomNavigationView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:background="?android:attr/windowBackground"
        android:backgroundTint="@color/colorTolbar"
        app:itemBackground="@drawable/button_selector"
        app:itemIconTint="@color/colorGray"
        app:itemTextColor="@color/colorGray"
        app:menu="@menu/navigation">

    </android.support.design.widget.BottomNavigationView>




</LinearLayout>
